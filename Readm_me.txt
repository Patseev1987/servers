В ветке master проект настроен без использования Docker. Docker используется только
для поднятия базы данных.

В ветке docker-compose проект настроен с использованием Docker, за исключение Jaws-server.
Так как происходит сохранение на локальную машину проекта.
